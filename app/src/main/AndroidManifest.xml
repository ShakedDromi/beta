<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beta">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beta">
        <activity android:name=".PMain"></activity>
        <activity android:name=".PDetails"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".BRegistr" />
        <activity android:name=".BDetails" />
        <activity android:name=".BMain" />
        <activity android:name=".about" />
        <activity android:name=".PRegister" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>